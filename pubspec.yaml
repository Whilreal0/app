name: nature_peaks
description: "A Flutter app with Supabase role-based authentication"
publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  shared_preferences: ^2.2.3
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.6
  dropdown_button2: ^2.3.9
  image_picker: ^1.0.4
  uuid: ^3.0.7
  



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env